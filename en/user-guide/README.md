# User guide

Every `sdx` command, on the router and on the server.

* [seedex-box](seedex-box.md) describes the router: services, rules, DNS, links, and the LuCI app.
* [seedex-agent](seedex-agent.md) describes the server: clients, protocols, and pairing.
